# Используем официальный образ C++ в качестве базового
FROM gcc:latest

# Устанавливаем рабочую директорию
WORKDIR /usr/src/app

# Копируем исходный код в контейнер
COPY . .

# Компилируем приложение
RUN g++ -o helloWorld main.cpp

# Запускаем приложение
CMD ["./helloWorld"]
